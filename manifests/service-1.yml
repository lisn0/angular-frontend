apiVersion: v1
kind: Service
metadata:
    name: "micro-b770"
    labels:
        app: "micro-b770"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "micro-b770"