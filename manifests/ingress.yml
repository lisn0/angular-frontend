apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "micro-156d"
    labels:
        app: "micro-156d"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-micro-156d.b199c1c52ede48eb94fb.eastus2.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "micro-156d"
                servicePort: 80
